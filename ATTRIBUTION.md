# Third-party assets

- **Kenney — Mini Characters, Blaster Kit** (models, animations, colormaps) — CC0 1.0 (public domain).
  https://kenney.nl · fetched from the GitHub mirror `shorepine/kenney`. Packed by `tools/pack-assets.js` into `src/06-assets.js`
  (embedded colormap stripped, one shared 512×512 palette per pack, only 10 animation clips kept, clips stored once and shared by all characters).
- **three.js r128** (MIT) — core via cdnjs; `GLTFLoader` and `SkeletonUtils` from `examples/js` inlined in `src/05-gltfloader.js`.
